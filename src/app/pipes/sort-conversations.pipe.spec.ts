import { SortConversationsPipe } from './sort-conversations.pipe';

describe('SortConversationsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortConversationsPipe();
    expect(pipe).toBeTruthy();
  });
});
